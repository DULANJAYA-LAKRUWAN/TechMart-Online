<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian
            http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <container qualifier="wildfly-remote" default="true">
        <configuration>
            <property name="managementAddress">${wildfly.host:localhost}</property>
            <property name="managementPort">${wildfly.port:9990}</property>
            <property name="username">${wildfly.user:admin}</property>
            <property name="password">${wildfly.password:admin123}</property>
        </configuration>
    </container>

</arquillian>
